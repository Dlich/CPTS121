#include "questions.h"

void main(void)
{
	char a[50] = "racecar";
	char b[50] = "ty";
	//Occurrences arr[128] = { 0 };
	//strcat(t, p);
	//my_str_n_cat(t, p);
	//int arr[256] = { 0 };
	//parrallel_array(p, arr);
	int ptr = 0;
	char c = '\0';
	for (int i = 0; i < 256; i++)
	{
		//printf("arr at %d = %d\n", i, arr[i]);
	}
	//printf("%d", count);
	//maximum_occurences(t, arr, &ptr, &c, 1);
	//printf("occurences: %d letter %c", ptr, c);
	//int arr[10] = { 2, 3, 7, 11, 15, 21, 25, 30, 37, 40 };
	//int index = binary_search(arr,40, 10);
	//rintf("%d", index);
	int x = is_palindrome(a, 7);
	printf("%d", x);
	//puts(my_str_n_cat(a, b));
}
